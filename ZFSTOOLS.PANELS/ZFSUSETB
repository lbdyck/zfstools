)Attr Default(%+#)
   ` type(output) intens(high) caps(off) just(left )
   ~ type(text) intens(high) color(turq)  hilite(reverse)
   @ type(text) intens(high) color(turq)
   $ type( input) intens(low ) caps(on ) just(left ) hilite(uscore)
   [ type(output) intens(high) caps(off) just(left ) color(turq)
   ! type(text) intens(high) color(blue)
   ] type(output) intens(high) caps(off) just(left ) color(blue)
   { type(output) intens(high) caps(off) just(left ) color(white)
)Body  Expand(\\)
%-\-\-~zFS Usage%-\-\-
%Command ===>#zcmd    \ \%Scroll ===>#zcsr+
%
%Commands:+Sort column order %Sel:%S+to Shrink,%G+to Grow, or anything to View
%
%S@Use        Owner    Attributes                       Parm
  !File_System
%  Mount_Point
)Model
$z[zuse    + [zown    [zatr                            [zprm
  ]zfs
  {zmnt
+-\-\-
)Init
.zvars = '(zsel)'
.help = zfsuseth
)Proc
)End
